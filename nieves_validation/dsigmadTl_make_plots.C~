#include <TStyle.h>
#include <TLatex.h>
#include "validation_costheta.C"

void plot_all_angles() {
  // Set paramters for plotting here
  TString graph_title = "LS_LFG_all_angles.png";
  double ymax = 25.0;
  TString rootFile = "LS_LFG_C12_E1GeV.gst.root";
  TString spline = "LS_LFG_C12_E1GeV.data.txt";
  TString target = "C12";
  double Enu = 1.0;
  TString RPA = "0";
  TString Coul = "0";
  
  double ctlminmax[]={-1.0,1.0};
  int ctlsize = 2;
  save_plot(graph_title,ymax,rootFile,spline,target,Enu,RPA,Coul,
	    ctlminmax,ctlsize);
  /*
  TString nievesName = target + "_" + EnuStr + "_R" + RPA + "_C" + Coul ;
  TString nievesData = nievesName + "/" + nievesName + "_all_angles.txt";
  TString plotTitle = "dSigma/dTl, " + target + 
    ", Enu = " + EnuStr + ", gevgen vs Nieves' fortran code";
  
  // Plot the result for the given angles
  TCanvas * c1 = new TCanvas("c1","c1",900,700);
  c1->SetLeftMargin(0.15);
  c1->SetBottomMargin(0.15);
  
  TH1D* hst_1 = validation_plot(ymax,rootFile,spline,target,Enu,
				-1.0,1.0,nievesData);

  // Add a legend
  TLegend* leg1 = new TLegend(.2,.7,.5,.9,"");
  leg1->AddEntry(hst_1,"dSigma/dEl","L");
  leg1->Draw();

  // Add a title
  gStyle->SetOptTitle(0);
  add_plot_label(plotTitle, 0.5,0.96);
  c1->SaveAs(target + "_" + EnuStr + "GeV_all_angles.png");
  //exit();*/
}

